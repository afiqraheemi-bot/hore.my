# Accounting Engine Technical Specification (AETS)

This directory contains the Accounting Engine Technical Specification series — the detailed technical design of hore.my's Accounting Core, the deterministic subsystem with exclusive authority over ledger posting ([ADR-0001](../../adr/0001-modular-monolith-architecture.md), [ADR-0004](../../adr/0004-financial-integrity-principles.md)).

Start with [`AETS-000.md`](AETS-000.md). It is the foundation of the series: purpose, scope, authority hierarchy, accounting and AI philosophy, design principles, document governance, versioning, and the planned structure of every later document. AETS-000 is architecture and governance only — it does not itself state accounting invariants or terminology; those are their own documents, below.

## Authority

AETS documents implement accepted ADRs; they do not create or override product or architectural decisions. Conflicts are resolved using the precedence in [`docs/product/reference/README.md`](../../product/reference/README.md) and [`docs/adr/README.md`](../../adr/README.md) — Founder instruction, then `HORE_MY_PROJECT_INSTRUCTIONS.txt`, then `HORE_MY_MASTER_CONTEXT.md`, then the SRS, then the Detailed Project Proposal, then accepted ADRs, then AETS. See AETS-000 §3 for the full statement.

## Lifecycle

Each document carries its own `Status`: `Draft` → `Active` → `Superseded` or `Deprecated`. A superseded document is kept, not deleted, and links to its replacement — the same rule ADRs follow. See AETS-000 §8.3.

## Index

| Document | Status | Version | Summary |
| --- | --- | --- | --- |
| [AETS-000 — Foundation](AETS-000.md) | Active | 1.2.1 | Purpose, scope, authority, philosophy, design principles, governance, versioning |
| [AETS-001 — Accounting Terminology](AETS-001-Accounting-Terminology.md) | Active | 1.3.0 | Canonical accounting vocabulary — 35 defined terms and terminology usage rules |
| [AETS-002 — Accounting Invariants](AETS-002-Accounting-Invariants.md) | Active | 2.0.0 | The 14 non-negotiable financial integrity invariants, extracted from AETS-000 §6 — invariant 3 distinguishes universal command idempotency from conditional source deduplication (v2.0.0, CTO/Accounting Domain Reviewer approved) |
| [AETS-003 — Money Specification](AETS-003-Money-Specification.md) | Active | 1.0.1 | Normative Money/Currency/MinorUnits domain model, public contract, persistence mapping, and 15 `MON-NNN` invariants |
| [AETS-004 — Journal & Posting Model](AETS-004-Journal-Posting-Model.md) | Active | 2.0.0 | Normative Journal/Journal Line model, debit/credit semantics, posting lifecycle, atomicity, idempotency, append-only rules, reversal/replacement, `financialDate`/`postedAt` semantics (M8), and 25 `JRN-NNN` invariants |
| [AETS-005 — Chart of Accounts & Account Taxonomy](AETS-005-Chart-of-Accounts.md) | Active | 1.0.0 | Normative Account model, code/type/normal-balance rules, hierarchy, posting eligibility, system vs. user-created accounts, tenant ownership, lifecycle, Journal Line integration, and 20 `COA-NNN` invariants |
| [AETS-007 — Accounting Commands & Posting Pipeline](AETS-007-Posting-Command.md) | Active | 2.1.0 | The concrete Posting Command contract (fields, identity/idempotency, Source Fingerprint, Actor/Source/Evidence relationship, Journal payload, required Financial Date, M8), atomic transaction/failure/success semantics, the AI-authority boundary, 28 `POST-NNN` invariants, and the fixed account mapping for every business-specific Accounting Command built to date (Expense, Income, Transfer, Owner Equity, Invoice Issuing, Payment Recording) plus the clarification that Payment Allocation posts no Journal and is not an Accounting Command at all |
| [AETS-008 — Bank Import, Matching & Reconciliation](AETS-008-Bank-Reconciliation.md) | Draft | 0.5.0 | Authority-locked Banking requirements, current implementation inventory, and — decided under explicit Founder delegation to the CTO (v0.2.0) and implemented/proven against real PostgreSQL (v0.3.0) — all nine §12 decision groups and invariants `BNK-014`–`BNK-020`; not effective pending Accounting Domain Reviewer sign-off. As of v0.4.0, §5.1 adds XLSX as a second accepted import file format for the identical fixed schema, sharing one validator with CSV. As of v0.5.0, §11's connected bank-statement segment is implemented and proven against AETS-012's candidate Golden Dataset, leaving Accounting Domain Reviewer sign-off as the sole remaining Activation blocker |
| [AETS-009 — Financial Reporting](AETS-009-Financial-Reporting.md) | Active | 1.8.0 | Read-only projection layer over Posted Journals: Trial Balance, Profit & Loss, Balance Sheet (with the "unclosed books" Cumulative Net Income convention), General Ledger drill-down, Evidence Index, (as of v1.1.0) the Debtors/Aging Report, and (as of v1.8.0) a Cash Flow Statement, plus CSV export for every report — Reconciliation Report remains explicitly deferred pending its prerequisite module — and 19 `RPT-NNN` invariants. As of v1.2.0–v1.4.0, Payment Allocation deletion is soft-delete (audit trail retained) and the Aging Report is fully point-in-time reproducible across both a later deallocation and a later-created allocation against a backdated Payment, with a dedicated cross-tenant isolation proof (§17). As of v1.5.0, §19 adds a Compliance Pack export — one ZIP bundling tenant-wide reports as CSV entries, explicitly not a compliance/audit-acceptance guarantee. As of v1.6.0, §20 adds XLSX export for every report, byte-for-byte consistent in content with the same report's own CSV output. As of v1.7.0, §21 adds a "loan-ready" formatted PDF export for Profit & Loss and Balance Sheet specifically (Invoice/Quotation PDF is a separate, resolved concern — see AETS-017; Trial Balance/General Ledger/Evidence Index/Aging PDF rendering remains deferred). As of v1.8.0, §22 resolves the long-deferred Cash Flow Statement (SRS RPT-003) by classifying each Journal's cash effect via its counterparty Account's own Account Type — reusing AETS-008's existing Bank Account marker for "is this Account cash," no change to AETS-005's Account Type contract — with Investing always RM0.00 pending a future Fixed Asset module; §21's PDF export and §19's Compliance Pack both extend to cover it, growing the Pack to nine entries (six CSV, three PDF) |
| [AETS-010 — Audit Trail & Evidence Linkage](AETS-010-Audit-Trail-Evidence-Linkage.md) | Active | 1.0.0 | The Audit Event schema (Tenant, Actor, Source, Audit Action, Subject, Time, applicable policy/model version) and the minimal Evidence Reference/Linkage contract deferred by AETS-004 §19/§26 and AETS-007 §10/§22 (§27 as of AETS-007 v2.1.0's renumbering; §26 at the time AETS-010 was written) — resolves the M4/M5 atomicity/auditability gap for ordinary Posting and Journal Correction, and 10 `AUD-NNN` invariants |
| [AETS-012 — Proof of Accuracy](AETS-012-Proof-of-Accuracy.md) | Draft | 0.3.0 | Proposed connected Golden Dataset contract and zero-tolerance certification gate — as of v0.2.0 its own domain-agnostic certification-harness mechanism (manifest/integrity verification, fail-closed aggregation) is implemented and proven, independent of any dataset. As of v0.3.0, a first candidate dataset (`hore-my-poa-v1` v1.0.0), its scenario runner/evaluator, and the `proof-of-accuracy:certify` command exist and every mechanical criterion POA-001–POA-010 passes against real PostgreSQL — `is_certified_passing` is structurally still `false`; not effective and not certified pending Accounting Domain Reviewer approval |
| [AETS-014 — Period Management](AETS-014-Period-Management.md) | Active | 1.2.0 | Period closing (an ever-advancing per-Tenant watermark, not discrete Period entities): the double-entry closing-entry construction zeroing every Revenue/Expense Account into a designated Retained Earnings Account, closed-period posting enforcement reused unchanged from the M4/AETS-007 Posting Command pipeline, and 9 `PER-NNN` invariants — reopening, discrete Periods/fiscal years, and automatic closing explicitly deferred. As of v1.2.0, §13 adds the minimal UI this capability had never had (a "Close Period" card on the Reports page, plus a `GET /api/v1/periods/current` watermark read endpoint) — resolving an audit-found gap where `close()` was fully built and tested but reachable only via a direct HTTP call |
| [AETS-015 — Evidence Storage](AETS-015-Evidence-Storage.md) | Draft | 0.1.0 | A first, narrow slice of AETS-010 §2.2's own named "Evidence Storage" deferral: a real, tenant-scoped, immutable Evidence aggregate and upload/download boundary — 7 `EVI-NNN` invariants; OCR/AI interpretation, Posting Command validation-pipeline wiring, encryption at rest, and retention policy all explicitly deferred; not effective pending Accounting Domain Reviewer sign-off |
| [AETS-016 — Quotation](AETS-016-Quotation.md) | Draft | 0.2.0 | Resolves AETS-001's own named "Quotation" terminology deferral: a real, tenant-scoped Quotation aggregate, structurally parallel to Invoice but producing zero ledger effect, moving through a five-state lifecycle (Draft/Sent/Accepted/Rejected/Converted) and converting atomically into a Draft Invoice once Accepted — 9 `QUO-NNN` invariants, including a genuine two-process concurrency proof for conversion; automatic expiry and Customer email delivery remain deferred; a minimal, functional PDF export is resolved (v0.2.0, see AETS-017) — branding/letterhead remains deferred; not effective pending Accounting Domain Reviewer sign-off |
| [AETS-017 — Invoice and Quotation PDF Export](AETS-017-Document-PDF-Export.md) | Draft | 0.1.1 | Resolves AETS-009's and AETS-016's identical "PDF (Invoice)"/"PDF quotation documents" deferrals with one shared, minimal, functional PDF template for both, rendered via Dompdf — seller identity from Business Profile, buyer identity from Customer, a line-item table, and a total; no logo, letterhead, or configurable color scheme (a tracked future enhancement) — 6 `PDF-NNN` invariants; PDF rendering of a report *statement* now has its own separate resolution for Profit & Loss/Balance Sheet (AETS-009 §21); as of v0.1.1, a mojibake bug in the shared template's footer (a literal em-dash, unrenderable by Dompdf's base font) is fixed; not effective pending Accounting Domain Reviewer sign-off |

This table, together with [AETS-000 §10](AETS-000.md#10-planned-document-structure), is the single authoritative roadmap for the AETS series — no other document states a competing numbering.

Planned, not-yet-created documents — AETS-006 (Posting Rules), AETS-011 (AI Accounting Proposal Contract), and AETS-013 (MyInvois Integration) — are listed in [AETS-000 §10](AETS-000.md#10-planned-document-structure). They are not reserved or committed to until actually created. AETS-008 and AETS-012 now exist as **Drafts** only; neither is effective, and AETS-012 does not mean Proof of Accuracy has passed. AETS-007 (Accounting Commands & Posting Pipeline) is `Active` (above), claiming in full [AETS-000 §10](AETS-000.md#10-planned-document-structure)'s "Accounting Commands" working title; its currently fully-specified content covers the Posting Command, plus (as of v2.1.0) the account mapping for every business-specific command actually built (AETS-007 §26) — a business-specific command not yet built (Bank Reconciliation, MyInvois, a Credit Note) remains a deferred future subsection of the same document (AETS-007 §1/§2/§27), not a separate AETS number. AETS-009 (Financial Reporting) is `Active` (above), claiming AETS-000 §10's "Financial Reporting" working title in full for the SRS-mandatory reports its current version supports (Trial Balance, Profit & Loss, Balance Sheet, General Ledger drill-down, Evidence Index, the Debtors/Aging Report and CSV export as of v1.1.0, and the Cash Flow Statement as of v1.8.0) — Reconciliation Report remains deferred (AETS-009 §2.2, §15), blocked on a specific, named future module, not a separate AETS number. AETS-010 (Audit Trail & Evidence Linkage) is `Active` (above), claiming AETS-000 §10's "Audit Trail" working title; its currently fully-specified content covers the Audit Event schema and the minimal Evidence Reference/Linkage contract only — Evidence's own full retention/storage schema is now a first, narrow slice resolved by AETS-015 (Draft, above); Evidence tenant-ownership *validation* (wiring the check into the Posting Command pipeline) and the Outbox Event schema remain deferred (AETS-010 §2.2, §11, §16), not resolved by this number. AETS-014 (Period Management) is `Active` (above), claiming AETS-000 §10's "Period Management" working title for Period *closing* only — reopening a closed Period, named/discrete Period entities, and automatic closing all remain deferred (AETS-014 §2.2), not resolved by this number.

## Test Specifications

`tests/` contains Accounting Test Specifications (ATS) — normative test specifications proving compliance with their corresponding AETS document. Each is numbered to match the AETS document it proves (e.g. `ATS-003` proves `AETS-003`). An ATS specifies what must be tested, at what level, and with what data; it is not implementation code.

| Document | Status | Version | Proves | Summary |
| --- | --- | --- | --- | --- |
| [ATS-003 — Money Test Specification](tests/ATS-003-Money-Test-Specification.md) | Active | 1.3.0 | [AETS-003](AETS-003-Money-Specification.md) | 95 test IDs (`MON-T001`–`MON-T095`) tracing every `MON-NNN` invariant to at least one test; golden MYR cases, property-based generators, persistence and vendor-isolation tests |
| [ATS-004 — Journal & Posting Test Specification](tests/ATS-004-Journal-Posting-Test-Specification.md) | Active | 1.6.0 | [AETS-004](AETS-004-Journal-Posting-Model.md) | 209 test IDs (`JRN-T001`–`JRN-T209`) tracing every `JRN-NNN` invariant to at least one test; balance/reversal property-based generators, golden journal cases, atomicity/idempotency/concurrency and tenant-isolation integration tests, Journal reconstitution (Draft/Posted restoration without a posting side effect), `financialDate`/`postedAt` construction, transition, and reconstitution-consistency tests (M8), real-PostgreSQL Journal persistence adapter tests (header/line mapping, exact `BIGINT` round-trip, malformed-value rejection), real-PostgreSQL production `journals`/`journal_lines` schema constraint tests (uniqueness, composite-FK same-Tenant Journal/Account integrity, canonical-value `CHECK` constraints, non-negative Money magnitude, migration reversibility), and real-PostgreSQL `JournalRepository` persistence tests (atomic header/line insert with fault-injected rollback, row-lock-based concurrency, Draft->Posted lifecycle persistence, and immutable persisted-Journal/Journal-Line protection) |
| [ATS-005 — Chart of Accounts Test Specification](tests/ATS-005-Chart-of-Accounts-Test-Specification.md) | Active | 1.6.0 | [AETS-005](AETS-005-Chart-of-Accounts.md) | 164 test IDs (`COA-T001`–`COA-T164`) tracing every `COA-NNN` invariant to at least one test; type/normal-balance and hierarchy property-based generators, golden account cases, tenant-isolation, posting-eligibility, Account deactivation-lifecycle, fail-closed incomplete-hierarchy-context, Account reconstitution, Account Origin (System/User-Created) construction and structural System Account protection, real-PostgreSQL Account persistence adapter tests, and real-PostgreSQL production `accounts` schema constraint tests (uniqueness, composite-FK tenant/parent integrity, self-parent and canonical-value `CHECK` constraints, migration reversibility) |
| [ATS-007 — Accounting Commands & Posting Pipeline Test Specification](tests/ATS-007-Posting-Pipeline-Test-Specification.md) | Active | 1.2.0 | [AETS-007](AETS-007-Posting-Command.md) | 140 test IDs (`POST-T001`–`POST-T140`) tracing every `POST-NNN` invariant to at least one test; Posting Command construction, tenant/Idempotency-Key/Source-Fingerprint/Actor-Source/Evidence/Account/Money-currency-balance/Financial-Date (M8) validation tests, duplicate-prevention and atomic-persistence tests (real-PostgreSQL fault injection at every required-write point), failure/success semantics, AI-authority-boundary parity tests, Audit Event and Outbox atomicity, no-network-in-transaction architecture proof, real-PostgreSQL concurrency tests, property-based generators, and 13 golden Posting Command scenarios — `POST-T093`–`POST-T095` (Audit Event) are now implemented against [AETS-010](AETS-010-Audit-Trail-Evidence-Linkage.md)/[ATS-010](tests/ATS-010-Audit-Trail-Test-Specification.md) (M6); `POST-T096`–`POST-T098` (Outbox) remain deferred, unchanged, since AETS-010 deliberately excludes the Outbox Event schema (§2.2) |
| [ATS-008 — Bank Import, Matching & Reconciliation Test Specification](tests/ATS-008-Bank-Reconciliation-Test-Specification.md) | Draft | 0.6.0 | [AETS-008](AETS-008-Bank-Reconciliation.md) | Inventory of 62 existing executable proofs — all ten of `BNK-014`–`BNK-020`'s items now implemented and proven (two-leg Transfer matching, period-overlap rejection, full-matching completion, immutable completion snapshot, replay/conflict concurrency, discrete Exact confidence, fail-closed negative balances, full matching-source coverage) — plus 1 deferred item (guided mapping, out of scope); not a release certificate. AETS-008 §5.1's new XLSX import format is covered by its own dedicated unit/integration/HTTP tests, described in AETS-008 §5.1 directly rather than a new `BNK-T0NN` row in this decision-tracking document. As of v0.6.0, `BNK-T057` (the connected golden dataset row) is implemented and passing against AETS-012's candidate dataset — stays `Required`, not `Existing`, pending Accounting Domain Reviewer approval |
| [ATS-009 — Financial Reporting Test Specification](tests/ATS-009-Financial-Reporting-Test-Specification.md) | Active | 1.8.0 | [AETS-009](AETS-009-Financial-Reporting.md) | 61 test IDs (`RPT-T001`–`RPT-T061`) tracing every `RPT-NNN` invariant to at least one test (`RPT-003` is a structural/architectural fact, not independently testable — see its own §7); Trial Balance, Profit & Loss, Balance Sheet, General Ledger drill-down, Evidence Index, and (as of v1.1.0–v1.4.0) Debtors/Aging Report unit and integration tests — including non-normal-Direction netting edge cases, aging-bucket-boundary edge cases, a no-write architecture proof, a dedicated cross-tenant Aging isolation test, real-PostgreSQL golden-dataset reconciliation against real Expense (M7)/Income (M9) postings covering Draft-exclusion, tenant isolation, `financialDate`-vs-`postedAt`, M5 correction-chain robustness, General Ledger traceability, and Evidence Index accuracy, (as of v1.5.0) a real-HTTP Compliance Pack ZIP-fidelity proof, (as of v1.6.0) a real-HTTP XLSX/CSV content-parity proof, (as of v1.7.0) a real-HTTP Profit & Loss/Balance Sheet PDF proof, and (as of v1.8.0) Cash Flow Statement unit/integration/HTTP tests — tie-out algebra, fail-closed classification on a hand-crafted ambiguous Journal, an empty-statement proof, a golden dataset spanning every supported transaction type, all three export formats, and tenant isolation |
| [ATS-010 — Audit Trail & Evidence Linkage Test Specification](tests/ATS-010-Audit-Trail-Test-Specification.md) | Active | 1.0.0 | [AETS-010](AETS-010-Audit-Trail-Evidence-Linkage.md) | 17 test IDs (`AUD-T001`–`AUD-T017`) tracing every `AUD-NNN` invariant to at least one test (`AUD-007` is a process constraint on upstream Evidence Reference production, not independently testable — see its own §7); Audit Event production for both M4 Posting and M5 Journal Correction, Evidence Reference construction and linkage, real-PostgreSQL atomicity fault-injection, replay behavior, tenant isolation, and schema constraint tests |
| [ATS-012 — Proof of Accuracy Test Specification](tests/ATS-012-Proof-of-Accuracy-Test-Specification.md) | Draft | 0.3.0 | [AETS-012](AETS-012-Proof-of-Accuracy.md) | 24 prospective tests (`POA-T001`–`POA-T024`) for artifact integrity, connected mandatory coverage, exact reports, deduplication, tenant isolation, repeatability, and fail-closed certification — as of v0.2.0, the harness mechanism behind `POA-T002`/`POA-T022`/`POA-T024` (and part of `POA-T001`) is implemented and proven with synthetic fixtures. As of v0.3.0, real-scenario coverage against the `hore-my-poa-v1` v1.0.0 candidate dataset closes or partially closes `POA-T003`–`T005`, `T007`, `T009`–`T015`, `T017`–`T021`, `T023`; `POA-T006`, `T008`, `T016` remain gaps; no passing-certification claim for the product |
| [ATS-014 — Period Management Test Specification](tests/ATS-014-Period-Management-Test-Specification.md) | Active | 1.1.0 | [AETS-014](AETS-014-Period-Management.md) | 28 concrete tests (`PER-T001`–`PER-T028`) tracing all 9 Period invariants across pure closing algebra, real-PostgreSQL lifecycle/atomicity/tenant/correction proofs, and isolated production-migration constraints/rollback; as of v1.1.0, a real-HTTP proof of the new watermark read endpoint (§6, not a new invariant) |

## Creating an AETS document

- Use the next sequential `AETS-NNN` number and a short, descriptive title.
- Copy AETS-000's header shape (`Status`, `Version`, `Effective date`, `Owner`, `Reviewers`, `Related`).
- Cite the ADR(s) and AETS-000/AETS-002 section(s) the document implements.
- Do not introduce anything [AETS-002](AETS-002-Accounting-Invariants.md) (accounting invariants) or AETS-000 §3 (authority hierarchy) prohibits.
- Add the document to the index table above once it exists.

## Creating an ATS document

- Name it `ATS-NNN-Title.md` under `tests/`, matching the AETS document number it proves.
- Copy an existing ATS's header shape (`Status`, `Version`, `Effective date`, `Owner`, `Reviewers`, `Related`).
- Use stable, prefixed test IDs (e.g. `MON-T001`) and trace every invariant of the AETS document it proves to at least one test ID.
- Do not invent behavior the corresponding AETS document does not define — flag ambiguity instead of guessing.
- Add the document to the Test Specifications index table above once it exists.
