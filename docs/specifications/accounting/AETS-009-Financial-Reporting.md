# AETS-009: Financial Reporting

- Status: Active
- Version: 1.9.0
- Effective date: 2026-09-06
- Owner: Accounting Core (see [`CODEOWNERS`](../../../CODEOWNERS))
- Reviewers: Founder / Product Owner; CTO / Technical Partner; Accounting Domain Reviewer
- Related: [AETS-000](AETS-000.md), [AETS-001](AETS-001-Accounting-Terminology.md), [AETS-002](AETS-002-Accounting-Invariants.md), [AETS-003](AETS-003-Money-Specification.md), [AETS-004](AETS-004-Journal-Posting-Model.md), [AETS-005](AETS-005-Chart-of-Accounts.md), [AETS-010](AETS-010-Audit-Trail-Evidence-Linkage.md); [ADR-0001](../../adr/0001-modular-monolith-architecture.md), [ADR-0004](../../adr/0004-financial-integrity-principles.md)

## 1. Purpose

This document is the normative specification for [AETS-000 §10](AETS-000.md#10-planned-document-structure)'s anticipated "AETS-009, Financial Reporting — Report derivation, rebuild-from-ledger guarantees." It defines the minimum set of financial reports SRS §4.9 requires as `Wajib` (mandatory) for MVP that this codebase's existing Accounting Core (M1–M8) and Transactions consumers (M7 Expense, M9 Income) can already support without depending on a module that does not yet exist.

Unlike [AETS-004](AETS-004-Journal-Posting-Model.md), [AETS-005](AETS-005-Chart-of-Accounts.md), and [AETS-007](AETS-007-Posting-Command.md), this document specifies a **read-only projection layer**, not a writer. It has no atomicity, idempotency, or concurrency contract of its own to define, because it never produces a ledger effect — [AETS-002 §4](AETS-002-Accounting-Invariants.md#4-the-invariants) invariant 5 ("Ledger-derived truth... Projections and reports are derived and must be rebuildable from posted journals — never an independent source of truth") is this document's entire posture in one sentence. This document is deliberately lighter than the write-side specs it depends on for exactly that reason.

This document uses **MUST**, **MUST NOT**, **SHOULD**, and **MAY** with their normal RFC 2119 meaning, exactly as [AETS-004 §1](AETS-004-Journal-Posting-Model.md#1-purpose) already establishes for this series.

## 2. Scope

### 2.1 In scope

Exactly the SRS §4.9 requirements this codebase's existing modules can support today, with no speculative design of a module that does not exist:

- **Imbangan Duga / Trial Balance** (SRS RPT-004) — §6.
- **Untung Rugi / Profit & Loss** (SRS RPT-001) — §7.
- **Kedudukan Kewangan / Balance Sheet** (SRS RPT-002) — §8.
- **Lejar Am / General Ledger drill-down** (SRS RPT-005) — §9.
- **Indeks Bukti / Evidence Index** (SRS RPT-008) — §10.
- The shared derivation rule every report in scope obeys: Tenant-scoped, Posted-Journal-only, exact Money arithmetic, rebuildable from the ledger alone (§5).
- **As of v1.1.0: Penghutang / Debtors and Aging Report** (SRS RPT-006) — §17, now that its prerequisite Invoice/Accounts-Receivable module (Invoicing, M20; Payments, M21) exists.
- **As of v1.1.0: Eksport CSV** (SRS RPT-009, CSV only) — §18, for every report this document defines.
- **As of v1.5.0: Compliance Pack export** — §19, a single downloadable ZIP bundling five of this document's own tenant-wide reports as CSV entries.
- **As of v1.6.0: Eksport XLSX** (SRS RPT-009, XLSX only) — §20, for every report this document defines, resolving the XLSX half of the deferral §2.2 previously stated in full.
- **As of v1.7.0: Eksport PDF "loan-ready"** (SRS RPT-009, PDF only) — §21, for Profit & Loss and Balance Sheet specifically: a formatted, human-readable statement a bank or accountant can review directly, resolving the loan-application-readiness half of Master Context §7 item 5 ("Untung rugi, kedudukan kewangan... boleh dieksport untuk semakan profesional"). Trial Balance, General Ledger, Evidence Index, and Aging Report PDF rendering remain deferred (§2.2, §15) — CSV/XLSX already serve those reports' own "exportable for professional review" purpose without a statement-format need.
- **As of v1.8.0: Aliran Tunai / Cash Flow Statement** (SRS RPT-003) — §22, resolving the deferral §2.2/§15 previously stated in full. Classifies each Journal's cash-touching effect by its **counterparty Account's own [AETS-005](AETS-005-Chart-of-Accounts.md) Account Type**, not by inventing a new field or scheme: Revenue/Expense/non-cash-Asset counterparties are Operating; Liability/Equity counterparties are Financing. Investing exists structurally but is always RM0.00 today — this codebase has no Fixed Asset module yet (§2.2, §15).

### 2.2 Out of scope

- **Laporan Rekonsiliasi / Reconciliation Report** (SRS RPT-007) — requires Bank Reconciliation ([AETS-008](AETS-000.md#10-planned-document-structure), not yet created). Deferred until that module exists.
- **Eksport PDF report rendering** — as of v1.7.0, resolved for Profit & Loss and Balance Sheet (§21): a bank loan officer or accountant reviewing a business's financial position expects a formatted statement, not a raw CSV/XLSX row export — CSV/XLSX serve *data interchange*, not *human review of a statement*, so this is a genuinely different need, not a redundant re-deferral of what §18/§20 already resolved. As of v1.9.0, this is fully resolved for every report this document defines: Trial Balance, General Ledger, Aging Report, and Evidence Index each also gain a PDF (§21), reopening the Founder's own explicit decision (direct instruction, 2026-09-20) to no longer treat "not a loan-review statement" as a reason to withhold a formatted export for a report a business owner or accountant may still want to print or file — a row-per-record table shape, distinct from the sectioned "statement" shape §21 already established for P&L/Balance Sheet/Cash Flow, but the same underlying already-computed data. Invoice/Quotation PDF is a separate, already-resolved concern — see [AETS-017](AETS-017-Document-PDF-Export.md).
- **Reproducibility as a formally tested guarantee** (SRS RPT-010) — SRS's own priority key marks this `Disyorkan` (recommended), not `Wajib`. This document's own rebuildability requirement (§5) already implies it structurally (a report with no stored, independent state is trivially reproducible from the same posted data), so no separate mechanism is invented merely to formally test what §5 already guarantees by construction.
- **Accounting Period entities, period close/reopen enforcement** — [AETS-014](AETS-000.md#10-planned-document-structure) (Period Management, not yet created) owns whether a period can be closed and whether closed-period posting is rejected. This document uses `financialDate` ([AETS-004 §9.1](AETS-004-Journal-Posting-Model.md#91-financial-date-and-posted-at)) as a plain date-range filter for a report — it does not require, and does not wait for, a formal Accounting Period aggregate to exist first.
- **Report performance/scaling architecture** (read replicas, caching, pre-aggregation) — [`HORE_MY_MASTER_CONTEXT.md`](../../product/reference/HORE_MY_MASTER_CONTEXT.md) §14's p95-under-two-seconds target is a non-functional requirement this document's derivation rules do not preclude meeting (a direct SQL aggregation over a solopreneur-scale ledger), but the document does not design caching or read-replica infrastructure — that remains an implementation/ops decision, revisited only if a real performance problem is measured.
- **Multi-currency reporting** — [AETS-004 §12](AETS-004-Journal-Posting-Model.md#12-balance-validation) already keeps multi-currency Journals out of MVP scope; every report in this document inherits that same single-Currency assumption without restating it as a new constraint.
- **Investing activity in the Cash Flow Statement (§22)** — the Investing section (§22) exists structurally but always totals RM0.00: no Fixed Asset module exists in this codebase yet, so no Account, and therefore no Journal, can ever be classified as an investing activity today. This is an honest, documented limitation of the *data available to classify*, not a defect in §22's own classification rule — revisited only once a Fixed Asset module is specified and built.
- Implementation code, ORM/schema design, and migrations — this is a specification, not code.

## 3. Authority

This document implements [ADR-0004](../../adr/0004-financial-integrity-principles.md)'s Ledger-derived-truth principle, within the terminology of [AETS-001](AETS-001-Accounting-Terminology.md), the invariants of [AETS-002](AETS-002-Accounting-Invariants.md) (specifically invariant 5), the Money contract of [AETS-003](AETS-003-Money-Specification.md), the Journal/Posting model of [AETS-004](AETS-004-Journal-Posting-Model.md), and the Account contract of [AETS-005](AETS-005-Chart-of-Accounts.md). It cannot weaken, override, or contradict any of them ([AETS-000 §3](AETS-000.md#3-authority-hierarchy)) — in particular, it never introduces a write path to `journals`, `journal_lines`, or `accounts`, and never treats a report's own computed output as authoritative in place of the ledger it was derived from.

Dependencies:

- [AETS-002](AETS-002-Accounting-Invariants.md) invariant 5 — the single governing principle this entire document elaborates.
- [AETS-003](AETS-003-Money-Specification.md) — every monetary total this document computes uses Money's own exact arithmetic (`add`, `subtract`, `compareTo`); no report ever uses native numeric arithmetic or a binary float.
- [AETS-004](AETS-004-Journal-Posting-Model.md) §6, §9, §9.1, §12 — the Journal aggregate, its lifecycle states (only `Posted` Journals contribute to any report, §5), and `financialDate` (the date every report filters or as-of's against).
- [AETS-005](AETS-005-Chart-of-Accounts.md) §10–§11 — the five Account Types and their Normal Balance, which every report in this document groups and nets by.
- [AETS-010](AETS-010-Audit-Trail-Evidence-Linkage.md) §8–§9 — the Evidence Reference/Linkage contract the Evidence Index (§10) reads, unchanged.

## 4. Definitions

Every term this document uses that [AETS-001](AETS-001-Accounting-Terminology.md) already defines carries exactly its AETS-001 meaning and is not redefined here.

- **Report** — a read-only projection computed fresh from Posted Journals and Accounts at the moment it is requested. A Report is never itself persisted as authoritative state; a stored cache of one, if ever introduced for performance, MUST remain invalidatable and re-derivable from the ledger without loss (§5).
- **As-of date** — the single Financial Date a point-in-time report (Trial Balance, §6; Balance Sheet, §8) is computed against: every Posted Journal with `financialDate <= as-of date` contributes; nothing later does.
- **Period** — the inclusive Financial Date range `[start, end]` a flow report (Profit & Loss, §7) or a ledger activity listing (General Ledger, §9) is computed against.
- **Net Balance** — an Account's total Debit Money minus total Credit Money, or the reverse, expressed as a single non-negative Money magnitude plus the `JournalDirection` of whichever side is larger — never a signed number, mirroring [AETS-004 §8](AETS-004-Journal-Posting-Model.md#8-debit-and-credit-semantics)'s own "magnitude-with-direction" resolution for a Journal Line. A Net Balance of exactly zero carries the zero Money magnitude and **no** Direction at all (neither side is larger) — this document never derives a Net Balance's Direction from an Account's Normal Balance, since `JournalDirection` and `NormalBalance` are, and remain, unrelated types with no conversion between them ({@see App\Domain\Accounting\Journal\JournalDirection}'s own established contract).

## 5. The shared derivation rule

Every report this document defines (§6–§10) MUST be computed under all of the following, without exception:

1. **Posted-only.** Only Journals whose `state` is `Posted` contribute. A Draft Journal has no ledger effect ([AETS-004 §9](AETS-004-Journal-Posting-Model.md#9-journal-states)) and MUST NOT appear in, or affect the total of, any report.
2. **Tenant-scoped.** Every report is computed for exactly one Tenant. A report MUST NOT aggregate, or leak the existence of, another Tenant's Journal, Journal Line, or Account ([AETS-002 §4](AETS-002-Accounting-Invariants.md#4-the-invariants) invariant 11).
3. **Rebuildable, never independently stored.** A report's numbers MUST be derivable, in full, from `journals`/`journal_lines`/`accounts` alone, at any time, by any two independent computations given the same inputs — producing identical output (this is what "rebuildable" means operationally: no report depends on a mutable running total, a cache with no invalidation path, or any state that could drift from the ledger). This is [AETS-002 §4](AETS-002-Accounting-Invariants.md#4-the-invariants) invariant 5, restated as this document's own binding rule, not a new one.
4. **Exact Money arithmetic throughout.** Every sum, difference, and comparison uses [AETS-003](AETS-003-Money-Specification.md)'s own exact operations (`Money::add()`, `Money::subtract()`, `Money::compareTo()`) — never native numeric arithmetic, never a binary float, at any point in any report's computation.
5. **Financial Date, never `postedAt` or `created_at`, is the reporting clock.** Every as-of/period filter in this document filters on [AETS-004 §9.1](AETS-004-Journal-Posting-Model.md#91-financial-date-and-posted-at)'s `financialDate` — the ledger-authoritative accounting date — never on `postedAt` (the system timestamp a Journal was durably posted) or any table's `created_at`. A Journal posted today for an effect dated last month reports against last month, exactly once, in the correct period.
6. **No write.** No report computation MUST ever insert, update, or delete a row in `journals`, `journal_lines`, `accounts`, or any other production table. A report's own presentation layer (§2.2, deferred) MAY cache its output for performance, but that cache is never itself a report's authoritative source.

## 6. Trial Balance (SRS RPT-004)

A Trial Balance, as of a given date, lists **every** Account belonging to the Tenant, together with its cumulative total Debit Money and total Credit Money summed across every Journal Line of every Posted Journal with `financialDate <= as-of date` referencing that Account — cumulative since the Account's own inception, never period-bounded, since a Balance Sheet-eligible Account's balance is a running stock, not a period flow ([AETS-001](AETS-001-Accounting-Terminology.md#balance)).

- An Account with zero contributing Journal Lines still appears, with a zero (directionless, §4) Net Balance — a Trial Balance lists the full Chart of Accounts, not only Accounts that happen to have activity.
- **The golden invariant (`RPT-T001` conceptually, formalized §12): total Debit across every Account line MUST exactly equal total Credit across every Account line.** This is not a Trial-Balance-specific fact invented here — it is the direct, structural consequence of [AETS-004 §7](AETS-004-Journal-Posting-Model.md#7-journal-line)'s own exact-balance requirement (`JRN-007`) holding for every individual Posted Journal; summing exactly-balanced Journals can never produce an imbalanced total. A Trial Balance that fails to balance indicates a defect in this report's own derivation, never a legitimate state of the ledger.
- SRS RPT-001's own acceptance criterion — "Golden report sama trial balance" — is satisfied structurally: the Profit & Loss's (§7) Revenue/Expense totals and the Balance Sheet's (§8) Asset/Liability/Equity totals are each a strict subset-and-regrouping of the exact same Trial Balance computation this section defines, never a separately-derived figure that could disagree with it.

## 7. Profit & Loss / Income Statement (SRS RPT-001)

A Profit & Loss statement, for a given Period `[start, end]`, lists every Revenue-type and Expense-type Account belonging to the Tenant, together with its total Debit Money and total Credit Money summed across every Journal Line of every Posted Journal with `financialDate` inside `[start, end]` (inclusive) referencing that Account — period-bounded, never cumulative since inception, since Revenue and Expense are inherently period flows ([AETS-001](AETS-001-Accounting-Terminology.md), Revenue/Expense terms).

- **Net Income** = (total Revenue Net Balance, Credit-direction magnitude) minus (total Expense Net Balance, Debit-direction magnitude), computed via `Money::compareTo()`/`subtract()` exactly as §4's Net Balance definition requires — never a signed native number. A positive result is a profit for the period; the reverse (Expense exceeding Revenue) is a loss, expressed as a non-negative Money magnitude with an explicit "loss" indicator, never a negative Money value (no such value can exist in this codebase's Money contract, [AETS-003](AETS-003-Money-Specification.md)).
- Every Revenue/Expense Account is grouped by its own [AETS-005](AETS-005-Chart-of-Accounts.md) hierarchy where one exists; this document does not invent a second grouping taxonomy.

## 8. Balance Sheet / Statement of Financial Position (SRS RPT-002)

A Balance Sheet, as of a given date, lists every Asset-type, Liability-type, and Equity-type Account belonging to the Tenant, at the same cumulative-since-inception Net Balance the Trial Balance (§6) already computes for each — a Balance Sheet is a strict subset-and-regrouping of a Trial Balance as of the same date, restricted to these three Account Types, never a separately-derived computation.

**The "unclosed books" equity line — a deliberate, named design decision, not an oversight.** No period-closing mechanism exists in this codebase yet ([AETS-014](AETS-000.md#10-planned-document-structure), Period Management, not yet created) — Revenue and Expense Accounts are never zeroed by a closing entry at a period boundary; they simply keep accumulating from account inception forever. Consequently, for the fundamental accounting identity **Assets = Liabilities + Equity** to hold at any as-of date, a Balance Sheet computed under this document MUST include one additional, computed Equity line — **Cumulative Net Income** — equal to the Profit & Loss (§7) computation for the Period `[account inception, as-of date]` (i.e. every Posted Journal ever, not just the most recent period). This is the standard "unclosed books" convention any double-entry system without period-closing must use; it is not a new accounting rule invented here, and it MUST NOT be confused with, or replace, a future Period Management module's own closing-entry mechanism (§2.2) — once that exists, this computed line is superseded by whatever real, posted closing entries that module produces, and this document (or a successor revision) will need to be revisited at that time.

- **The golden invariant (`RPT-T00X` conceptually, formalized §12): total Assets MUST exactly equal total Liabilities plus total Equity (including the Cumulative Net Income line above).** Any other outcome indicates a defect in this report's own derivation — it can never legitimately arise from a ledger every one of whose Journals independently balances (§6's own reasoning applies transitively).

## 9. General Ledger drill-down (SRS RPT-005)

For a single Account and a given Period `[start, end]`, the General Ledger listing enumerates, in `financialDate` order (ties broken by `postedAt`, never the reverse), every Journal Line of every Posted Journal referencing that Account whose `financialDate` falls inside the Period — each entry carrying its Journal's stable identifier, `financialDate`, `postedAt`, the Line's own Money amount and Direction, and the Journal's Source reference ([AETS-004 §19](AETS-004-Journal-Posting-Model.md#19-actor--source--evidence-traceability)) and any linked Evidence References ([AETS-010 §8–§9](AETS-010-Audit-Trail-Evidence-Linkage.md#8-evidence-reference--minimal-contract)) — so a user can trace any number on any other report in this document back to the exact Journal, and from there to its original evidence, satisfying SRS RPT-005's own acceptance criterion ("Setiap baris dijejak" — every line is traced).

- An opening balance (the Account's Net Balance as of the day immediately before the Period's `start`, per §6's own cumulative computation) and a running balance after each listed entry MAY be included as a convenience; neither is itself authoritative — both remain fully re-derivable from the same underlying Journal Lines (§5).
- This report never surfaces a Draft Journal or a Journal belonging to a different Tenant (§5).

## 10. Evidence Index (SRS RPT-008)

For a given Period `[start, end]`, the Evidence Index lists every Posted Journal belonging to the Tenant with `financialDate` inside the Period, together with: its Source reference, whether it carries at least one linked Evidence Reference ([AETS-010 §9](AETS-010-Audit-Trail-Evidence-Linkage.md#9-evidence-linkage)), and, where present, each linked Evidence Reference's own opaque value — satisfying SRS RPT-008's own acceptance criterion ("Bukti hilang dikenal pasti" — missing evidence is identifiable).

- A Journal with zero linked Evidence References is not itself a defect (per [AETS-010 §9](AETS-010-Audit-Trail-Evidence-Linkage.md#9-evidence-linkage), a pure Reversal or a purely-manual command legitimately carries none) — this report surfaces the fact plainly; it does not classify or flag which absences are "expected" versus "concerning," since that judgment is a Transactions-domain or user-facing concern this document does not decide.
- This report reads [AETS-010](AETS-010-Audit-Trail-Evidence-Linkage.md)'s existing Evidence Reference/Linkage contract entirely unchanged — it does not add a field to it, and does not require Evidence's own still-deferred full schema (AETS-010 §2.2) to exist.

## 11. Tenant isolation and security

Every report defined in this document MUST be requested with, and computed for, exactly one Tenant — there is no "all Tenants" report, and no report parameter can widen its own scope beyond the Tenant it was requested for. Application, query, and database controls MUST prevent cross-tenant access to any report's underlying data, consistent with [AETS-004 §18](AETS-004-Journal-Posting-Model.md#18-tenant-isolation) and [AETS-002 §4](AETS-002-Accounting-Invariants.md#4-the-invariants) invariant 11 — the identical requirement every write-side document in this series already states, restated here for the read side.

## 12. Invariants

Each invariant below is Financial-Reporting-specific, additional to [AETS-002](AETS-002-Accounting-Invariants.md)'s 14 invariants and to this series' existing `JRN-NNN`/`COA-NNN`/`AUD-NNN` invariants, which each already state the underlying ledger fact a report merely reads.

| ID | Invariant |
| --- | --- |
| RPT-001 | **Posted-only.** No report MUST include, or be affected by, a Draft Journal (§5). |
| RPT-002 | **Tenant-scoped.** No report MUST aggregate or reveal another Tenant's data (§5, §11). |
| RPT-003 | **Rebuildable.** Every report MUST be derivable, in full, from `journals`/`journal_lines`/`accounts` alone, with no independent stored source of truth (§5). |
| RPT-004 | **Exact Money arithmetic.** No report MUST use native numeric arithmetic or a binary float at any point in its computation (§5). |
| RPT-005 | **Financial Date is the reporting clock.** No report MUST filter or group by `postedAt` or `created_at` in place of `financialDate` (§5). |
| RPT-006 | **No write.** No report computation MUST perform an insert, update, or delete against `journals`, `journal_lines`, or `accounts` (§5). |
| RPT-007 | **Trial Balance exact balance.** A Trial Balance's total Debit MUST exactly equal its total Credit, for any Tenant, at any as-of date (§6). |
| RPT-008 | **Balance Sheet exact balance.** A Balance Sheet's total Assets MUST exactly equal its total Liabilities plus total Equity (including the Cumulative Net Income line, §8), for any Tenant, at any as-of date. |
| RPT-009 | **Golden-report consistency.** A Profit & Loss's and a Balance Sheet's own totals MUST each be a strict subset-and-regrouping of the same Trial Balance computation for the same Tenant and date/Period — never a separately-derived figure that could disagree with it (§6, §7, §8). |
| RPT-010 | **General Ledger traceability.** Every entry the General Ledger drill-down lists MUST resolve back to a real, Posted Journal Line belonging to the Tenant and Account requested (§9). |
| RPT-011 | **No fabricated Evidence status.** The Evidence Index MUST report a Journal's true linked-Evidence state exactly as [AETS-010](AETS-010-Audit-Trail-Evidence-Linkage.md) records it — never inferring, assuming, or fabricating an Evidence Reference that was not actually linked (§10). |
| RPT-012 | **Aging completeness.** The Debtors/Aging report MUST list every Issued Invoice belonging to the Tenant with a non-zero outstanding balance as of the requested date exactly once, in exactly one bucket; a fully-paid, Draft, or not-yet-issued (as of that date) Invoice MUST NOT appear (§17). |
| RPT-013 | **Aging bucket determinism.** An Invoice's aging bucket MUST be computed solely from whole days overdue (as-of date minus due date), never from an Account's Normal Balance, a Journal Line's Direction, or any other unrelated signal; the same (Invoice, as-of date) pair MUST always resolve to the same bucket (§17). |
| RPT-014 | **Aging historical reproducibility across time.** A Debtors/Aging report computed for a given as-of date MUST return an identical result no matter when it is computed — neither a Payment Allocation's later deallocation nor its later creation may change a report for an as-of date that precedes it. A deallocation MUST affect only as-of dates on or after the date it took effect; an allocation created after a given as-of date MUST NOT count toward that date's own result, even if the Payment it allocates is itself dated before that as-of date (§17). |
| RPT-015 | **Compliance Pack fidelity.** Every CSV entry inside a Compliance Pack ZIP (§19) MUST be byte-identical to what that same report's own `?format=csv` endpoint would return for the identical parameters — the bundle introduces no new computation, no new row mapping, and no divergent formatting of its own (§19). |
| RPT-016 | **XLSX/CSV content parity.** For identical parameters, a report's `?format=xlsx` cell values MUST equal its own `?format=csv` row values exactly, cell for cell — both are built from the identical `(header, rows)` tuple (§20); neither format may compute or round a value the other does not. |
| RPT-017 | **PDF/JSON figure parity.** Every line amount and total shown in a Profit & Loss or Balance Sheet PDF export (§21) MUST equal the same figure that report's own `?format=json` (default) response returns for identical parameters — the PDF introduces no new computation, only a different rendering of already-computed `AccountBalance`/`Money` values. |
| RPT-018 | **Cash Flow tie-out.** `Cash at Period End` MUST exactly equal `Cash at Period Start` plus `Net Change in Cash`, for any Tenant, at any Period (§22). |
| RPT-019 | **Cash Flow classification fails closed.** A Journal whose counterparty Lines span more than one Account Type MUST cause the Cash Flow Statement's derivation to fail with an explicit error — it MUST NOT silently classify it into any one activity, average its amounts, or drop a Line (§22). |
| RPT-020 | **Extended PDF/JSON figure parity, plus General Ledger running-balance tie-out.** Every figure shown in a Trial Balance, Aging Report, or Evidence Index PDF export (§21) MUST equal the same figure that report's own `?format=json` (default) response returns for identical parameters — the same no-new-computation guarantee `RPT-017` already states, extended to these three. General Ledger's PDF export additionally introduces one new derived value not present in any other format: a running balance per row, computed by accumulating each entry's already-computed amount and Direction onto the previous row's balance, in the same order `entries()` already returns them, starting from the report's own already-computed opening balance. The final row's running balance MUST exactly equal the report's own already-computed closing balance — a tie-out proven directly against real PostgreSQL, mirroring `RPT-018`'s own Cash Flow tie-out (§21). |

## 13. ATS Requirements

This section states what a future Financial Reporting Test Specification (`ATS-009`, numbered to match this document) MUST prove; it does not write that test specification.

A Financial Reporting ATS MUST include:

- **Invariant traceability** — every `RPT-NNN` invariant (§12) traced to at least one test, mirroring this series' own established traceability-matrix pattern.
- **Golden dataset reconciliation** — a representative range of posted Journals (including Expense, M7, and Income, M9, postings) whose Trial Balance, Profit & Loss, and Balance Sheet are computed and checked against hand-verified expected totals, proving `RPT-007`–`RPT-009` — property-based where practical (a generated range of balanced Journal sets, each proven to still balance in the resulting Trial Balance and Balance Sheet), rather than fault-injection/concurrency-style tests, since this document has no write path to fault-inject against.
- **Draft-exclusion proof** — a Draft Journal present in the database does not appear in, or affect the total of, any report (`RPT-001`).
- **Tenant isolation proof** — a report requested for Tenant A never reflects Tenant B's Journals or Accounts, even when both share overlapping Account identifiers or dates (`RPT-002`).
- **Financial-Date-vs-postedAt proof** — a Journal posted on one date for an effect dated on a materially different date reports against its `financialDate`, never its `postedAt` (`RPT-005`), proven directly against real PostgreSQL.
- **Reversal/Replacement correctness** — a Reversed or Replaced Journal chain (M5) still nets to the structurally correct Trial Balance/Balance Sheet totals (the reversal's neutralizing effect, plus the replacement's corrected effect, sum exactly as expected) — proving this document's reports require no special-casing for corrections, only summing every Posted Journal that exists.
- **General Ledger traceability proof** — every listed entry resolves back to a real Journal Line, in the correct order, with the correct Source/Evidence references attached (`RPT-010`).
- **Evidence Index accuracy proof** — a Journal with linked Evidence appears with it; a Journal with none appears with an explicit absence, never a fabricated reference (`RPT-011`).
- **Real PostgreSQL integration tests** — every claim above proven against a real PostgreSQL instance, never SQLite, consistent with this series' own established precedent.
- **No-write architecture proof** — a static/architectural test proving no report class's source contains an `insert`/`update`/`delete` call against any production table (`RPT-006`), mirroring `JRN-T032`/`POST-T022`'s existing no-network/no-side-effect technique.
- **As of v1.1.0: Aging completeness and bucket determinism proof** — a fully-paid, Draft, and not-yet-issued Invoice each proven absent from the Aging report; an Invoice at each bucket boundary (0, 1, 30, 31, 60, 61, 90, 91+ days overdue) proven to land in the correct bucket; a partially-allocated Invoice's outstanding balance proven to reflect only allocations from Payments made on or before the as-of date (`RPT-012`, `RPT-013`, §17).
- **As of v1.4.0: Aging historical reproducibility proof, both boundaries** — a fully-allocated Invoice's report result for a historical as-of date proven identical before and after the contributing allocation is later deallocated; a not-yet-allocated Invoice's report result for a historical as-of date proven identical before and after a late allocation is made against an earlier-dated Payment; both proven to differ only for an as-of date on or after the deallocation/allocation itself (`RPT-014`, §17). Plus a dedicated two-Tenant Aging isolation proof, closing the last named gap in §15.
- **As of v1.5.0: Compliance Pack fidelity proof** — every one of the five bundled CSV entries proven byte-identical to that same report's own `?format=csv` output for identical parameters; the archive proven to open as a valid ZIP containing exactly those five named entries (`RPT-015`, §19).
- **As of v1.6.0: XLSX/CSV content parity proof** — a report's `?format=xlsx` cells proven to open as a valid workbook whose values match the identical report's own `?format=csv` output cell for cell, for at least one report with real posted activity (`RPT-016`, §20).
- **As of v1.7.0: PDF/JSON figure parity proof** — a Profit & Loss and a Balance Sheet PDF, each proven to open as a real PDF (magic-byte check) whose Content-Type is `application/pdf`, for real posted activity (`RPT-017`, §21).
- **As of v1.8.0: Cash Flow golden-dataset and tie-out proof** — a golden dataset exercising every supported transaction type (Expense, Income, Loan received, Loan repayment, Owner Capital contribution, Owner Drawing, and a Cash↔Bank transfer) proven to classify each into the correct activity section with the correct amount and direction, and `Cash at Period End` proven to exactly equal `Cash at Period Start` plus `Net Change in Cash` (`RPT-018`, §22). A hand-crafted Journal whose counterparty Lines span more than one Account Type proven to throw `AmbiguousCashFlowClassificationException` rather than silently misclassify (`RPT-019`, §22) — via a real-PostgreSQL integration test, since no real command in this codebase can produce such a Journal. A Tenant with no registered Bank Account proven to receive an entirely empty statement, never an error. Tenant isolation proven exactly as `RPT-002` requires for §6–§10.
- **As of v1.9.0: Extended PDF proof, plus General Ledger running-balance tie-out** — Trial Balance, General Ledger, Aging Report, and Evidence Index PDF exports each proven to open as a real PDF (magic-byte check) whose Content-Type is `application/pdf`, for real posted activity (`RPT-020`, §21). A dedicated proof, both as an isolated unit test against hand-crafted mixed-Debit/Credit entries and as a real-PostgreSQL integration test against real posted Expense/Income activity, that the General Ledger PDF's per-row running balance accumulates correctly and that its final row exactly equals the report's own already-computed closing balance (`RPT-020`, §21).

## 14. Examples (Informative)

These examples are illustrative only and are not normative.

- A Tenant posts one Expense (Debit `ACCOUNT-OFFICE-SUPPLIES` RM50.00, Credit `ACCOUNT-CASH` RM50.00) and one Income (Debit `ACCOUNT-CASH` RM200.00, Credit `ACCOUNT-CONSULTING-REVENUE` RM200.00) within the same month. That month's Profit & Loss shows Revenue RM200.00, Expense RM50.00, Net Income RM150.00 (profit). A Balance Sheet as of month-end shows `ACCOUNT-CASH` at a net Debit Net Balance of RM150.00, and an Equity side carrying a Cumulative Net Income line of RM150.00 — Assets (RM150.00) exactly equal Liabilities (RM0.00) plus Equity (RM150.00).
- The Expense above is later reversed and replaced with a corrected RM80.00 amount (M5). The Trial Balance as of any date after the replacement reflects the net combined effect of the original, its reversal, and the replacement — RM80.00 of expense — with no special-case logic in this document's own reports; the correction chain is simply more Posted Journals to sum.

## 15. Deferred Items

- ~~**Cash Flow Statement**~~ (SRS RPT-003) — resolved 2026-09-17; see §22. Classified via counterparty Account Type, reusing AETS-008's own Bank Account marker for "is this Account cash" — no change to AETS-005's Account Type contract. Investing remains structurally always RM0.00 pending a future Fixed Asset module (§2.2) — an honest limitation of available data, not of the classification rule itself.
- ~~**Debtors and Aging report**~~ — resolved; see §17. Its prerequisite Invoice/Accounts-Receivable module (Invoicing, M20; Payments, M21) now exists.
- **Reconciliation Report** (SRS RPT-007) — deferred pending Bank Reconciliation, AETS-008 (§2.2).
- ~~**PDF rendering of Profit & Loss / Balance Sheet**~~ — resolved 2026-09-17; see §21. Trial Balance, General Ledger, Evidence Index, and Aging Report PDF rendering remain deferred — CSV (§18) and XLSX (§20) already serve those reports' own review purpose without a "statement" format need. Invoice/Quotation PDF is a separate, already-resolved concern — see [AETS-017](AETS-017-Document-PDF-Export.md).
- **Formal reproducibility testing** (SRS RPT-010, `Disyorkan`) — implied structurally by §5's rebuildability rule; not separately tested by name (§2.2).
- ~~**Aging Report historical reproducibility across time**~~ — resolved 2026-09-11, in two passes (P1-3/P1-4 for the deletion side; a same-day audit follow-up for the creation side); see §17. A Payment Allocation is no longer hard-deleted, and `AgingReportQuery` now compares both `deleted_at` and `created_at` against the requested as-of date rather than merely checking `deleted_at IS NULL`, so a historical as-of-date's result is identical no matter when the report is re-run, whether a contributing allocation is later deallocated or only created later.
- ~~**Aging Report cross-tenant isolation test**~~ — resolved 2026-09-11; see `AgingReportQueryIntegrationTest::test_two_tenants_never_leak_into_each_others_aging_report()` and `RPT-T048` (ATS-009). Two Tenants sharing the same MYR amount and dates (but distinct Account/Invoice/Payment IDs, since `accounts.account_id` is that table's own primary key) are proven not to leak into each other's report or outstanding balance.
- ~~**Period Management integration**~~ — resolved; see [AETS-014](AETS-014-Period-Management.md), which specifies Period closing. No change to this document's own Balance Sheet computation was needed: a closing Journal's zeroing lines are ordinary Posted Journal Lines, so `ProfitAndLossQuery::forPeriod([inception, asOfDate])`'s existing "since inception" aggregation automatically nets a closed range's original activity against that same range's closing entries to zero, leaving the "unclosed books" Cumulative Net Income line correctly reflecting only activity since the last closing — verified directly (`PeriodClosingServiceIntegrationTest::test_the_unclosed_books_convention_correctly_shows_only_post_closing_activity`).
- **Report caching/performance infrastructure** — read replicas, pre-aggregation, or caching, should a real performance problem be measured against the p95 target ([`HORE_MY_MASTER_CONTEXT.md`](../../product/reference/HORE_MY_MASTER_CONTEXT.md) §14) — not designed speculatively here (§2.2).
- ~~**This document's own future ATS-009 update for §17/§18**~~ — [ATS-009](tests/ATS-009-Financial-Reporting-Test-Specification.md) traces `RPT-012`–`RPT-014` to the Aging test suite, including the cross-tenant test above (`RPT-T048`), as of its own v1.4.0.

## 16. Change Governance

This document follows [AETS-000](AETS-000.md)'s governance rules in full — it does not restate them. Lifecycle, review requirements, and versioning (`MAJOR.MINOR.PATCH` with a changelog note for any `Active`-document change, [AETS-000 §9.1](AETS-000.md#91-per-document-version)) all apply unchanged.

A change to any `RPT-NNN` invariant, or to any MUST-level requirement in §5–§11, is a MAJOR change under that rule. Adding a new report definition for a capability already `Wajib` in SRS but currently deferred here (§2.2) — once its prerequisite module exists — is at minimum a MINOR change; it MUST NOT weaken or contradict any invariant this version already establishes.

## 17. Debtors and Aging Report (SRS RPT-006)

For a given as-of date, the Debtors/Aging report lists every Issued Invoice belonging to the Tenant with a non-zero outstanding balance as of that date, each assigned to exactly one aging bucket based on how overdue its due date is relative to the as-of date — resolving the deferral §2.2/§15 previously stated pending "an Invoice/Accounts-Receivable module." That module (Invoicing, M20; Payments, M21) now exists, and this section specifies the report exactly as already implemented, per [AETS-000 §9.1](AETS-000.md#91-per-document-version)'s rule that resolving a named deferred item is itself at minimum a MINOR change.

**Outstanding balance**, for an Invoice as of a given date, is its `totalAmount` minus the sum of every Payment Allocation against it where the allocating Payment's own `paymentDate <= as-of date` — never the full allocation total regardless of date, since a Payment made after the as-of date cannot retroactively have settled a debt as of that date.

**Aging buckets** are computed from whole days overdue (`as-of date` minus `dueDate`), never from an Account's Normal Balance, a Journal Line's Direction, or any other unrelated signal (`RPT-013`):

| Days overdue | Bucket |
| --- | --- |
| ≤ 0 (not yet due, or due exactly on the as-of date) | Current |
| 1–30 | Overdue 1 to 30 days |
| 31–60 | Overdue 31 to 60 days |
| 61–90 | Overdue 61 to 90 days |
| 91+ | Overdue 91+ days |

- A Draft Invoice never appears — only `Issued` Invoices are eligible (`RPT-012`).
- An Invoice issued after the as-of date never appears, regardless of status.
- A fully-paid Invoice (outstanding balance exactly zero as of the as-of date) contributes no line (`RPT-012`).
- The report's grand total, and each bucket's own subtotal, are the exact sum (via `Money::add()`, never native arithmetic) of every listed line's own outstanding balance (§5 rule 4, applied here unchanged).
- Tenant isolation (§11) applies unchanged: a report requested for one Tenant never reflects another Tenant's Invoices, Payments, or Allocations — though see §15's own note on this specifically not yet having a dedicated test, unlike `RPT-002`'s proof for §6–§10.

**This report's relationship to §5's shared derivation rule.** Rules 1 (Posted-only, read as "only an Issued Invoice's own posted effects count" here), 2 (Tenant-scoped), 4 (exact Money arithmetic), and 6 (no write) apply to this report exactly as to every other (§6–§10). This report additionally reads `invoices` and `payment_allocations` directly — tables §5 did not originally anticipate a report reading from (§6–§10 read only `journals`/`journal_lines`/`accounts`, plus, for §9–§10, `audit_events`/`journal_evidence_links`) — because an outstanding balance is a receivables-subledger fact, not a Journal Line sum: [AETS-007 §26.7](AETS-007-Posting-Command.md#267-payment-allocation--not-an-accounting-command-m21) already establishes that Payment Allocation posts no Journal at all, so no Journal Line exists for this report to sum in the first place. Rule 5 (Financial Date as the reporting clock) is honored in spirit — this report filters on Invoice's own `issue_date` and Payment's own `payment_date`, the same accounting-date fields [AETS-007 §26.5–§26.6](AETS-007-Posting-Command.md#265-invoice-issuing-command-m20) already established as each command's own Financial Date — but not by a Journal's `financialDate` field directly, for the same reason.

**A formerly-named limitation, resolved in two passes (2026-09-11: P1-3/P1-4 for the deletion side; a same-day audit follow-up for the creation side this document's own v1.3.0 changelog entry prematurely called "fully resolved").** Rule 3 (rebuildable) holds exactly as stated — two independent computations of this report against the *same* database state, for the same Tenant and as-of date, always produce identical output, **and now this holds across time too, on both boundaries an allocation has**: a *past* as-of date's result is identical no matter when the report is re-run, whether a contributing allocation is later deallocated, or a contributing allocation is only created later against an earlier-dated Payment.

*Deletion side (P1-3/P1-4).* `PaymentAllocation` deletion ({@see App\Domain\Payments\AllocationService::deallocate()}) is a soft delete (`deleted_at`/`deleted_by_actor`) rather than a physical `DELETE` — a deallocated allocation's full history is permanently retained, and `AgingReportQuery` compares `deleted_at` against the requested as-of date rather than merely checking `IS NULL`: an allocation counts toward a given as-of date if it had not yet been deallocated as of that date — `deleted_at IS NULL`, or `deleted_at`'s own calendar date is *after* the as-of date.

*Creation side (found by an external audit the same day, after this document's v1.3.0 had already shipped claiming full resolution).* An allocation additionally only counts toward a given as-of date if it was itself *created* on or before that date — `payment_allocations.created_at`'s own calendar date must not be after the as-of date. Without this, a Payment dated in the past but only allocated to an Invoice much later would make a historical report retroactively show that Invoice as settled the moment the late allocation was made — the exact scenario the audit demonstrated concretely (a Payment dated 5 August, allocated only in September, silently rewriting an Aging report for 10 August).

Both `created_at` and `deleted_at` are system timestamps, not user-supplied Financial Dates, but neither use conflicts with rule 5: each answers "had this fact taken effect yet, as of this date," never substitutes for a Financial Date on a financial event — an allocation, unlike a Payment or an Invoice, has no business-dated field of its own. This is a deliberate, conservative policy choice between two the audit itself named as open (a system-effective timestamp cutoff, chosen here, vs. a not-yet-designed business-effective allocation date) — see this document's own §16 change-governance rule for how the latter, if ever pursued, would require its own MAJOR revision, not a silent reinterpretation of `RPT-014` below.

Verified directly, for both boundaries independently: an Invoice paid in full, queried as of a historical date, then deallocated, then re-queried for that *same* historical date, returns an identical result both times, with only a report for the *current* date reflecting the deallocation; and an Invoice with a backdated Payment but no allocation yet, queried as of a historical date (fully outstanding), then allocated at real "now," then re-queried for that *same* historical date, again returns an identical (still fully outstanding) result, with only a report for the *current* date reflecting the late allocation.

- Implemented by [`AgingReportQuery`](../../../apps/api/app/Infrastructure/Invoicing/Reporting/AgingReportQuery.php), returning an [`AgingReport`](../../../apps/api/app/Domain/Invoicing/Reporting/AgingReport.php) of [`AgingReportLine`](../../../apps/api/app/Domain/Invoicing/Reporting/AgingReportLine.php)s, each assigned an [`AgingBucket`](../../../apps/api/app/Domain/Invoicing/Reporting/AgingBucket.php).

## 18. Export presentation — CSV (SRS RPT-009, CSV portion only)

Resolving, for CSV specifically, the deferral §2.2/§15 previously stated in full for "PDF/XLSX/CSV": every report this document defines (§6–§10, §17, §22) MAY be requested in CSV form instead of JSON, via a `format=csv` request parameter, unchanged in every other respect — the same Query-layer computation (§5) runs; only the HTTP-layer presentation differs.

- **No new business logic.** CSV rendering reshapes an already-computed report's own existing fields into rows and a header line; it introduces no new report field, no new computation, and no new validation rule. §5's entire derivation rule (Posted-only, Tenant-scoped, rebuildable, exact Money arithmetic, Financial-Date-governed, no write) governs the underlying computation exactly as for the JSON form — CSV is a pure reshaping of the same output.
- **Money values render as exact decimal strings** (`Money::toDecimalString()`), never a native float — the same representation the JSON form already uses.
- PDF *report* rendering remains deferred (§15) — Invoice/Quotation PDF is resolved separately, see [AETS-017](AETS-017-Document-PDF-Export.md). XLSX is resolved; see §20.
- Implemented by [`CsvResponseBuilder`](../../../apps/api/app/Http/Support/CsvResponseBuilder.php), invoked from [`ReportingController`](../../../apps/api/app/Http/Controllers/Api/ReportingController.php) for every report this document defines.

## 19. Compliance Pack export (added v1.5.0; PDF entries added v1.7.0; Cash Flow entries added v1.8.0)

Resolves the operative half of Master Context §7's "Compliance-ready pack" item — the only concrete requirement that sentence states: *"rekod tersusun, konsisten, boleh dijejaki dan boleh dieksport untuk semakan profesional"* (organized, consistent, traceable, and exportable records for professional review). This section makes that literal: one downloadable ZIP archive bundling six of §2.1's own tenant-wide reports as CSV entries, plus three of them again as §21 "loan-ready" PDF statements, for a caller-supplied period — a bank or accountant handed this Pack gets a human-readable statement alongside the machine-readable data, not only the latter.

- **Bundled reports (CSV):** Trial Balance (as of the period's end date), Profit & Loss (for the period), Balance Sheet (as of the period's end date), Aging (as of the period's end date), Evidence Index (for the period), and (as of v1.8.0) Cash Flow (for the period) — every tenant-wide report §2.1 currently defines except General Ledger, which requires a specific Account rather than a tenant-wide view and so has no single canonical CSV to bundle.
- **Bundled statements (PDF):** Profit & Loss and Balance Sheet (as of v1.7.0), and (as of v1.8.0) Cash Flow, each in the identical §21/§22 "loan-ready" format the standalone `?format=pdf` endpoint already produces for it.
- **No new business logic, no new report.** Every CSV entry is produced by calling the exact same Query class and the exact same row-mapping §18's own `?format=csv` path already uses for that report (`RPT-015`); every PDF entry is rendered by the exact same `DocumentPdfBuilder`/view §21 already uses for the standalone endpoint (`RPT-017`). This section defines a presentation-layer combination only; it introduces no new computation and cannot diverge from what each report's own existing endpoint would return for the same parameters.
- **Not a compliance guarantee.** Mirroring Master Context §7's own second sentence verbatim: *"Ia bukan jaminan automatik bahawa audit, cukai atau penyerahan akan diterima"* (it is not an automatic guarantee that an audit, tax filing, or submission will be accepted). This section names an export bundle, not a certification, an audit opinion, or a submission-readiness claim of any kind.
- **Format.** A single `.zip` file (MIME type `application/zip`), containing exactly nine entries named `trial-balance.csv`, `profit-and-loss.csv`, `balance-sheet.csv`, `aging-report.csv`, `evidence-index.csv`, `cash-flow.csv`, `profit-and-loss.pdf`, `balance-sheet.pdf`, and `cash-flow.pdf`.
- Implemented by [`ZipResponseBuilder`](../../../apps/api/app/Http/Support/ZipResponseBuilder.php) (a domain-agnostic ZIP-bundling presentation helper, mirroring `CsvResponseBuilder`'s own scoping) wrapping `CsvResponseBuilder::toCsvString()`'s own output, invoked from `ReportingController::compliancePack()`.

## 20. Export presentation — XLSX (added v1.6.0)

Resolves the XLSX half of the deferral §2.2 previously stated in full for "PDF/XLSX": every report this document defines (§6–§10, §17, §22) MAY be requested in XLSX form instead of JSON, via `format=xlsx`, exactly mirroring §18's own CSV mechanism — the same already-computed `(header, rows)` tuple §18 builds is handed to `XlsxResponseBuilder` instead of `CsvResponseBuilder`, so the two formats can never diverge in content (`RPT-016`).

- **No new business logic**, for the identical reason §18 states for CSV — this section reshapes already-computed output only.
- **Every cell is written as an exact string, never a native spreadsheet number.** A Money amount rendered as a real Excel numeric cell risks silent floating-point misrepresentation the moment a user's own spreadsheet recomputes it; writing the identical `Money::toDecimalString()` value CSV already uses keeps the two formats byte-for-byte consistent in content.
- PDF report rendering remains deferred (§15) for the reason stated there.
- Implemented by [`XlsxResponseBuilder`](../../../apps/api/app/Http/Support/XlsxResponseBuilder.php), invoked from `ReportingController` alongside `CsvResponseBuilder` for every report this document defines.

## 21. Export presentation — PDF (added v1.7.0 as "loan-ready" for P&L/Balance Sheet; extended to Cash Flow v1.8.0; extended to every remaining report v1.9.0)

Resolves the PDF half of the deferral §2.2 previously stated in full. As of v1.9.0, **every report this document defines has a PDF export** — Profit & Loss (§7), Balance Sheet (§8), and Cash Flow (§22) in the "loan-ready" statement shape (v1.7.0/v1.8.0, unchanged below); Trial Balance (§6), General Ledger (§9), Aging Report (§17), and Evidence Index (§10) in a row-per-record table shape (v1.9.0, new). The v1.9.0 extension is an explicit Founder decision (direct instruction, 2026-09-20) reopening §2.2's own prior reasoning that CSV/XLSX already served these four reports' needs — the Founder judged a formatted, directly-printable/reviewable export still has value for all seven, not only the three a bank loan review specifically turns on.

**The "loan-ready" statement shape (P&L, Balance Sheet, Cash Flow — unchanged from v1.7.0/v1.8.0):**

- A formatted, human-readable statement — business name/address header, sectioned line items (Revenue/Expenses for P&L; Assets/Liabilities/Equity for Balance Sheet; Operating/Investing/Financing for Cash Flow), subtotals, and a final Net Income / Total Assets / Net Change in Cash figure — the shape a bank loan officer or accountant expects to review directly, as opposed to a raw data row export (§18/§20 already serve that need). It is **not** a claim of loan approval, audit acceptance, or any third-party endorsement — the same honesty convention already established for "compliance-ready" (Master Context §7) and for AETS-017's own PDF documents.
- One shared template for all three reports (`resources/views/pdf/financial-statement.blade.php`), mirroring AETS-017 §4's identical reasoning for Invoice/Quotation: they differ only in section labels and figures, never the underlying layout.

**The row-per-record table shape (Trial Balance, General Ledger, Aging Report, Evidence Index — new v1.9.0):**

- None of these four is a subtotaled-sections "statement" the way P&L/Balance Sheet/Cash Flow are — each is a flat list of records (an Account row, a ledger entry, an aging invoice, an evidence-status entry). A second shared template, `resources/views/pdf/tabular-report.blade.php`, renders an arbitrary N-column header/row table plus optional note lines and summary totals — structurally closer to AETS-017's own Invoice/Quotation line-item table than to the sectioned statement template above, reusing the identical header/footer chrome and CSS conventions.
- **Trial Balance** shows real, separate Debit/Credit columns per Account (`AccountBalance::totalDebit()`/`totalCredit()`, already computed, never collapsed to a single net figure the way the statement template does for P&L/Balance Sheet), plus a `Balanced: Yes/No` note and Total Debit/Total Credit summary lines.
- **Aging Report** shows a resolved Customer name (a new, presentation-only `customerNamesByTenant()` bulk lookup, mirroring the existing account-name lookup below — `AgingReportLine` itself carries only an opaque `CustomerId`, unchanged) alongside invoice number, due date, a human bucket label, and outstanding balance, plus each bucket's own total and the grand total as summary lines.
- **Evidence Index** shows a resolved human description per entry via the existing `SourceDescriptionLookup` (already built for this report's own JSON `description` field; reused here, not duplicated) alongside date, source type, and evidence presence, plus a summary note counting how many entries have evidence attached.
- **General Ledger** shows a resolved human description per entry via the same `SourceDescriptionLookup`, plus (the one genuinely new derived value across all seven PDF reports) a per-row running balance — see `RPT-020` (§12) for its exact tie-out guarantee.
- **No new business logic beyond the running balance (`RPT-017`/`RPT-020`).** Every other figure rendered is the identical already-computed value the same report's own JSON/CSV/XLSX response already returns; an account/customer name lookup and a source description lookup are the only other HTTP-layer additions, each a plain, uncomputed read of the Tenant's own reference data (`accounts`, `customers`) or already-existing infrastructure (`SourceDescriptionLookup`).

**Applies to both shapes:**

- **No company logo, no letterhead, no configurable color scheme** — the same Founder-level branding deferral AETS-017 §3 already states, extended here rather than re-litigated.
- Requested via `format=pdf` on every report endpoint this document defines: `/api/v1/reports/profit-and-loss`, `/balance-sheet`, `/cash-flow`, `/trial-balance`, `/general-ledger`, `/aging`, and `/evidence-index`. `GeneralLedgerRequest`'s own validation rule now accepts `pdf` too (previously rejected it as an invalid value, per v1.7.0's own note — that gap is closed).
- Implemented by the existing [`DocumentPdfBuilder`](../../../apps/api/app/Http/Support/DocumentPdfBuilder.php) (AETS-017) — no new PDF rendering infrastructure, the same Dompdf pipeline every PDF in this codebase already uses, now rendering one of two shared Blade views depending on report shape.

## 22. Cash Flow Statement (SRS RPT-003; added v1.8.0)

Resolves the deferral §2.2/§15 previously stated in full for v1.0.0's own reasoning: a Cash Flow Statement requires an operating/investing/financing activity classification scheme [AETS-005](AETS-005-Chart-of-Accounts.md)'s five-Account-Type contract does not itself define. This section resolves that without amending AETS-005 or its Account Type contract at all — classification is derived entirely from data that already exists.

**Cash-equivalent Accounts.** An Account is cash-equivalent for this report if, and only if, it is the `linkedAccountId` of an `active` Bank Account row ([AETS-008](AETS-000.md#10-planned-document-structure), Bank Reconciliation's own existing marker of which Accounts represent real cash/bank balances). This document does not invent a second "is this Account cash" concept — it reuses AETS-008's own.

**Classification rule.** For a given Period `[start, end]`, this report considers every Posted Journal with at least one Line against a cash-equivalent Account and `financialDate` inside `[start, end]`. For each such Journal:

- If **every** Line is cash-equivalent (a transfer between two of the Tenant's own cash-equivalent Accounts), the Journal is excluded entirely — moving money between two bank Accounts is not itself an operating, investing, or financing activity; it has no effect on total cash and would double-count if included.
- Otherwise, every non-cash-equivalent ("counterparty") Line on that Journal MUST belong to Accounts of exactly one [AETS-005](AETS-005-Chart-of-Accounts.md) Account Type. That Account Type then classifies the Journal's entire cash effect:
  - **Revenue, Expense, or (non-cash-equivalent) Asset** counterparty → **Operating activity**.
  - **Liability or Equity** counterparty → **Financing activity**.
  - No Account Type in this codebase today classifies as **Investing** — see §2.2's own note; the Investing section is always empty (RM0.00) until a Fixed Asset module exists.
- **Fail closed on ambiguity (`RPT-019`).** A Journal whose counterparty Lines span more than one Account Type — which no command this codebase currently translates can produce, since every existing translator emits a simple two-Line Journal — MUST cause this report's derivation to fail with an explicit error, never silently pick one Account Type, average the amounts, or drop a Line. A report that cannot classify a Journal unambiguously MUST NOT guess.

**Net Cash Flow per line**, and each activity section's own subtotal, is a Net Balance (§4) exactly as every other report in this document computes one — Debit-direction (mirroring Cash's own Asset Normal Balance) means a net cash inflow attributable to that counterparty Account; Credit-direction means a net outflow. No new signed-Money type is introduced.

**Cash at Period Start / Cash at Period End** are each the combined cash-equivalent Accounts' own Net Balance (§6's Trial Balance computation, restricted to cash-equivalent Accounts) as of the day immediately before the Period's `start`, and as of the Period's `end`, respectively — the same cumulative-since-inception computation §6/§8 already define, not a new one.

- **The golden invariant (`RPT-018`): `Cash at Period End` MUST exactly equal `Cash at Period Start` plus `Net Change in Cash`, where `Net Change in Cash` is computed by summing every Operating, Investing, and Financing line together (never by summing the three already-netted section totals — the identical anti-double-netting reasoning §7's own Net Income computation already applies).** This is not a Cash-Flow-specific fact invented here: because every Posted Journal this report considers touches at least one cash-equivalent Account by construction (§5's own rebuildability rule, applied here), the sum of every counterparty Line's net effect is structurally guaranteed to equal the cash-equivalent Accounts' own actual balance change over the same Period.
- A Tenant with no registered (`active`) Bank Account gets an entirely empty statement — zero lines in every section, RM0.00 cash at both boundaries — never an error; this report has no cash-equivalent Account to derive anything from, which is a valid, honestly-reported state, not a defect.
- This report's relationship to §5's shared derivation rule: rules 1 (Posted-only), 2 (Tenant-scoped), 4 (exact Money arithmetic), 5 (Financial Date as the reporting clock), and 6 (no write) apply exactly as to every other report (§6–§10, §17). Unlike §6–§10, this report is deliberately implemented as a **self-contained** query, not an extension of the shared Account-balance aggregation machinery §6–§8 share — isolating this new, v1.8.0 capability from any risk to the four already-`Active`, already-governed reports (Trial Balance, Profit & Loss, Balance Sheet, General Ledger) that machinery serves.
- Export presentation (§18 CSV, §20 XLSX, §21 PDF) and Compliance Pack inclusion (§19) all apply to this report exactly as to Profit & Loss and Balance Sheet — see each section's own v1.8.0 changelog note.
- Implemented by [`CashFlowStatementQuery`](../../../apps/api/app/Infrastructure/Accounting/Reporting/CashFlowStatementQuery.php), returning a [`CashFlowStatement`](../../../apps/api/app/Domain/Accounting/Reporting/CashFlowStatement.php) of [`CashFlowLine`](../../../apps/api/app/Domain/Accounting/Reporting/CashFlowLine.php)s grouped by [`CashFlowActivity`](../../../apps/api/app/Domain/Accounting/Reporting/CashFlowActivity.php), and throwing [`AmbiguousCashFlowClassificationException`](../../../apps/api/app/Domain/Accounting/Reporting/Exception/AmbiguousCashFlowClassificationException.php) per the fail-closed rule above.

## Changelog

- **1.9.0 (2026-09-20):** Extends **§21, Export presentation — PDF** to the four reports v1.7.0 had left deferred: Trial Balance, General Ledger, Aging Report, and Evidence Index — resolving §2.2's PDF deferral in full, per an explicit Founder decision (direct instruction, 2026-09-20) reopening that deferral's own prior "CSV/XLSX already serve these four" reasoning. A new shared `resources/views/pdf/tabular-report.blade.php` view renders these four (a row-per-record table, structurally distinct from the sectioned "statement" shape `financial-statement.blade.php` already serves for P&L/Balance Sheet/Cash Flow) — Trial Balance gains real Debit/Credit columns (`AccountBalance::totalDebit()`/`totalCredit()`, previously collapsed to a single net figure by the statement template); Aging Report gains a resolved Customer name via a new, presentation-only `customerNamesByTenant()` lookup mirroring the existing account-name one; General Ledger and Evidence Index each gain a resolved human description per entry via the existing `SourceDescriptionLookup` (built for Evidence Index's own JSON `description` field, reused here). Introduces `RPT-020`: the same PDF/JSON figure-parity guarantee `RPT-017` already states, extended to these three, plus General Ledger's own genuinely new derived value — a per-row running balance, computed by accumulating each entry's already-computed amount+Direction onto the previous row's balance (starting from the report's own already-computed opening balance, in the same chronological order `entries()` already returns them), proven to tie out exactly to the report's own already-computed closing balance, both as an isolated unit test (`GeneralLedgerRunningBalanceTest`) and a real-PostgreSQL integration test (`ReportingQueriesIntegrationTest`) — mirroring `RPT-018`'s own Cash Flow tie-out precedent. `GeneralLedgerRequest`'s own validation rule now accepts `pdf` (previously rejected it as invalid, the one gap v1.7.0's own note had named). Compliance Pack (§19) is deliberately **not** extended to bundle these four — General Ledger still has no tenant-wide CSV to bundle (§19's own existing reasoning, unchanged), and bundling the other three was not part of what this change resolves; revisiting §19 remains a separate future decision. No existing `RPT-NNN` invariant's prior meaning changed, no previously specified report definition altered. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version) and this document's own §16 rule: resolves a named deferred item (§2.2) now that the Founder has explicitly authorized it, introducing one new invariant without weakening or contradicting any existing one.
- **1.8.0 (2026-09-17):** Adds **§22, Cash Flow Statement**, resolving SRS RPT-003's deferral stated since v1.0.0. Classification is derived entirely from data that already exists — no amendment to [AETS-005](AETS-005-Chart-of-Accounts.md)'s Account Type contract: a Journal's counterparty (non-cash-equivalent) Lines' own Account Type decides its activity (Revenue/Expense/Asset → Operating; Liability/Equity → Financing), and "cash-equivalent" reuses AETS-008's own existing Bank Account `linkedAccountId`/`active` marker rather than inventing a new one. Investing exists structurally but is always RM0.00 today, pending a not-yet-built Fixed Asset module (§2.2) — a documented data-availability limitation, not a defect. Introduces `RPT-018` (Cash Flow tie-out: `Cash at Period End` = `Cash at Period Start` + `Net Change in Cash`) and `RPT-019` (fail-closed classification: a Journal whose counterparty Lines span more than one Account Type throws rather than silently misclassifying — provable only via a hand-crafted integration test, since no real command in this codebase currently produces such a Journal). Implemented as a self-contained query, deliberately not an extension of the shared Account-balance aggregation §6–§8 depend on, to keep this new capability's risk isolated from those four already-`Active` reports. Extends §18 (CSV), §20 (XLSX), and §21 (PDF, via the existing shared `financial-statement.blade.php` view) to cover this report, and extends §19's Compliance Pack from seven entries to nine (adding `cash-flow.csv` and `cash-flow.pdf`). No existing `RPT-NNN` invariant's prior meaning changed, no previously specified report definition altered. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version) and this document's own §16 rule: resolves a named deferred item (§2.2/§15) now that no prerequisite is missing, introducing two new invariants without weakening or contradicting any existing one.
- **1.7.0 (2026-09-17):** Adds **§21, Export presentation — PDF ("loan-ready")**, resolving the PDF half of SRS RPT-009's deferral for Profit & Loss and Balance Sheet specifically — a formatted statement a bank or accountant can review directly, distinct from CSV/XLSX's data-interchange purpose (§18/§20). Trial Balance, General Ledger, Evidence Index, and Aging Report PDF remain deferred, unchanged from v1.6.0's own reasoning. Introduces `RPT-017` (PDF/JSON figure parity: the PDF introduces no new computation). Implemented via the existing `DocumentPdfBuilder` (AETS-017) and a new shared `financial-statement.blade.php` view — no new PDF infrastructure. Also extends **§19's Compliance Pack** with the same two PDF statements, growing it from five CSV entries to seven (five CSV, two PDF) — resolving Master Context §7 item 7's ("annual tax/accountant export pack") need for a human-readable statement alongside the existing machine-readable data, no new computation there either. Updates §2.1/§2.2/§15 to reflect the narrower (not full-report-suite) PDF resolution. No `RPT-NNN` invariant's prior meaning changed. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version).
- **1.6.0 (2026-09-16):** Adds **§20, Export presentation — XLSX**, resolving the XLSX half of SRS RPT-009's "Eksport PDF, XLSX dan CSV" deferral for every report this document defines — the identical `(header, rows)` tuple §18 already builds for CSV, handed to a new `XlsxResponseBuilder` instead. Introduces `RPT-016` (XLSX/CSV content parity). Clarifies, across §2.1/§2.2/§15/§18, that PDF *rendering of a financial report itself* remains the only deferred item now — Invoice/Quotation PDF (a structurally separate, customer-facing document) is resolved by the new [AETS-017](AETS-017-Document-PDF-Export.md), not by this document. No `RPT-NNN` invariant's prior meaning changed. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version).
- **1.5.0 (2026-09-16):** Adds **§19, Compliance Pack export** — a single downloadable ZIP bundling Trial Balance, Profit & Loss, Balance Sheet, Aging, and Evidence Index as CSV entries for a caller-supplied period, resolving the operative half of Master Context §7's "Compliance-ready pack" item (organized, consistent, traceable, exportable records — explicitly restated as never a guarantee of audit/tax/submission acceptance). Introduces `RPT-015` (Compliance Pack fidelity: every bundled CSV byte-identical to that report's own existing `?format=csv` output). No new report, no new computation, no new dependency beyond the `zip` PHP extension the bundling itself requires; PDF and XLSX export remain deferred exactly as §2.2/§15 already state. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version) and this document's own §16 rule: a new presentation-layer combination of already-specified reports, introducing no obligation that weakens or contradicts any existing `RPT-NNN` invariant.
- **1.4.0 (2026-09-11):** Same-day follow-up to v1.3.0, which had prematurely classified §17's own limitation as "fully resolved" — an independent external audit review of that commit found the fix only closed the deletion-side half of the gap. `AgingReportQuery` now additionally compares `payment_allocations.created_at` against the requested as-of date: an allocation created after a given as-of date no longer counts toward that date's own result, even when the Payment it allocates carries an earlier date — closing the exact scenario the audit demonstrated (a Payment dated 5 August, allocated only in September, retroactively rewriting a 10 August report). Broadens `RPT-014`'s own wording to state both boundaries explicitly rather than only the deletion one. Also closes the last item §15 had named as an open test-coverage gap: a dedicated two-Tenant Aging isolation test (two Tenants sharing identical amounts/dates but distinct fixture IDs, proven not to leak). Verified: new integration tests for both the creation-boundary and cross-tenant scenarios, PHPStan 0 errors, Pint clean, full regression passing deterministically. No `RPT-NNN` invariant's prior meaning changed beyond broadening `RPT-014`'s own stated scope to match what it was always intended to guarantee; no previously specified report definition altered. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version): completes an already-declared invariant's coverage and closes a named test gap, does not introduce a new obligation.
- **1.3.0 (2026-09-11):** Fully resolved §17's own named limitation (Aging Report historical reproducibility), completing what v1.2.0's P1-3 fix only partially closed. `AgingReportQuery` now compares `deleted_at` against the requested as-of date — an allocation counts if `deleted_at IS NULL` or its calendar date is after the as-of date — instead of unconditionally excluding every soft-deleted allocation regardless of date. Rewrites §17's own limitation paragraph and §15's bullet to state the limitation is resolved rather than partially open. Adds `RPT-014` (Aging historical reproducibility across time) to §12, and a corresponding ATS-009 coverage bullet to §13. Verified: a new integration test (`AgingReportQueryIntegrationTest::test_a_historical_as_of_date_stays_reproducible_after_a_later_deallocation`) proves a historical as-of-date's result is bit-for-bit identical before and after a later deallocation, and a real E2E run against the live stack (curl: issue Invoice, record Payment, allocate, query historical as-of date, deallocate, re-query the *same* historical date, confirm identical result, then query today's date and confirm it *does* reflect the deallocation) confirms the same, not just the test suite. No `RPT-NNN` invariant's prior meaning changed, no previously specified report definition altered. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version): resolves a named limitation with a new invariant, does not alter any existing one.
- **1.2.0 (2026-09-11):** Partially resolved §17's own named limitation (Aging Report historical reproducibility) as part of P1-3 audit remediation: `PaymentAllocation` deletion ({@see App\Domain\Payments\AllocationService::deallocate()}) changed from a physical `DELETE` to a soft delete (`deleted_at`/`deleted_by_actor`), closing the audit-trail gap the limitation's root cause named. The Aging Report query (`AgingReportQuery`) was updated to filter `deleted_at IS NULL`, preserving its exact prior observable behavior (a deallocated allocation still never counts toward the current outstanding balance) — proven by a new regression test asserting a deallocated Invoice reappears as fully outstanding, exactly as the old hard-delete produced. This does **not** fully resolve the limitation: the query still does not compare `deleted_at` against the requested as-of date, so a historical as-of-date figure can still change across time if a contributing allocation is later deallocated — §17 and §15 are both reworded to state precisely what is now resolved (data retention) versus what remains open (the report's own point-in-time logic), rather than leaving the original, now-partially-stale wording in place. No `RPT-NNN` invariant, report definition, or other previously specified MUST-level requirement changed. Classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version): a clarification of an already-named limitation's current status, not a new or altered obligation.
- **1.1.0 (2026-09-08):** Resolved a governance gap an external audit surfaced: M22 (Aging Report) and M23 (CSV export) had both shipped, with real Query/Controller code and real tests, without this document ever being updated to specify either — §2.2/§15 still described both as fully deferred, pending prerequisites that had in fact already been built. Adds new **§17, Debtors and Aging Report**, specifying the outstanding-balance computation, the five aging buckets, and this report's relationship to §5's shared derivation rule (including one explicitly named, currently-unresolved limitation: historical reproducibility across time is not guaranteed once a contributing Payment Allocation is later deallocated, since deallocation is a hard delete with no retained history). Adds new **§18, Export presentation — CSV**, resolving the CSV portion only of SRS RPT-009 for every report this document defines; PDF and XLSX remain deferred. Adds `RPT-012` (Aging completeness) and `RPT-013` (Aging bucket determinism) to §12, and a corresponding ATS-009 coverage bullet to §13. Updates §2.1/§2.2/§15 to move Debtors/Aging and CSV export from deferred to specified, while explicitly recording two residual open items §15 now names for the first time: the historical-reproducibility limitation above, and the absence of a dedicated cross-tenant isolation test for the Aging Report specifically (unlike `RPT-002`'s existing proof for §6–§10). No existing report definition, `RPT-NNN` invariant, or previously specified MUST-level requirement in §5–§11 changed — classified **MINOR** per [AETS-000 §9.1](AETS-000.md#91-per-document-version) and this document's own §16 rule ("adding a new report definition... once its prerequisite module exists — is at minimum a MINOR change").
- **1.0.2 (2026-09-07):** Editorial: §15's Period Management deferral is resolved — [AETS-014](AETS-014-Period-Management.md) now exists, and this document's own Balance Sheet computation required no code change (verified directly). No invariant or previously specified behavior changed.
- **1.0.1 (2026-09-07):** Editorial: §15's forward-reference to this document's own future ATS-009 now links to that document, which has since been written. No invariant, MUST-level requirement, or previously specified behavior changed.
- **1.0.0 (2026-09-06):** Initial creation. Specifies Trial Balance (RPT-004), Profit & Loss (RPT-001), Balance Sheet (RPT-002), General Ledger drill-down (RPT-005), and Evidence Index (RPT-008) — the five SRS §4.9 `Wajib` reports this codebase's existing Accounting Core (M1–M8) and Transactions consumers (M7, M9) can support without depending on a not-yet-built module. Cash Flow (RPT-003), Debtors/Aging (RPT-006), and Reconciliation Report (RPT-007) are explicitly deferred by name (§2.2, §15), each blocked on a specific, named future module — not silently dropped. Introduces `RPT-001`–`RPT-011`. Documents the "unclosed books" Cumulative Net Income convention (§8) as a deliberate, named design decision pending AETS-014 (Period Management). Reviewed and marked `Active` per the Founder's M10 Architecture Review approval (GO for this exact scope; RPT-003/006/007 explicitly NO-GO pending their prerequisite modules).
